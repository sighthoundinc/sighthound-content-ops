version: '3'

vars:
  DEFT_ROOT: '{{joinPath .TASKFILE_DIR ".."}}'

tasks:
  check:
    desc: Verify required toolchain is installed (go, uv, task, git, gh)
    env:
      PYTHONUTF8: "1"
    cmds:
      - uv --project "{{.DEFT_ROOT}}" run python "{{.DEFT_ROOT}}/scripts/toolchain-check.py"
