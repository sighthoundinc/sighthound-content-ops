"""tests/cmd_gate/ -- regression tests for the universal-gate state surfaces.

Empty package marker; mirrors the convention from ``tests/contract/`` and
``tests/relocate/`` so pytest discovery treats this as a regular Python
package. Test modules under this directory cover ``cmd_gate`` /
``_check_upgrade_gate`` state-detection helpers in ``run``.
"""
